// to push our code to github

git init
git add .
git commit -m "MESSAGE"
git branch -M main 
git remote add origin "github link"
git remote -v
git push -U origin main

// end

// npm commands
npm i express
npm i nodemon

mkdir router,controller,models

